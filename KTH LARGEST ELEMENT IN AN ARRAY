class Solution(object):
    def findKthLargest(self, nums, k):
        heap=[]
        for i in nums:
            heapq.heappush(heap,-i)
        for i in range(k):
            c=heapq.heappop(heap)
        return -c
