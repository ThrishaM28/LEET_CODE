class Solution(object):
    def pivotIndex(self, nums): 
        l=0
        for i in range(len(nums)):
            r=sum(nums)-nums[i]-l
            if l==r:
                return i
            l=l+nums[i]
        return -1
