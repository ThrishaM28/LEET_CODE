class Solution(object):
    def isValid(self, s):
        l=[]
        d={')':'(','}':'{',']':'['}
        for i in s:
            if (i in d.values()):
                l.append(i)
            else:
                if(not l or d[i]!=l.pop()):
                    return False
        return len(l)==0
