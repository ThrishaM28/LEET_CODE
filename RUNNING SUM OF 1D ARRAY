class Solution(object):
    def runningSum(self, nums):
        res=[]
        res.append(nums[0])
        for i in range(1,len(nums)):
            d=nums[i]+res[-1]
            res.append(d)
        return res
